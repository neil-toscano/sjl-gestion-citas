export interface TimeRemaining {
  expired: boolean; // Indica si el tiempo ha expirado
  days: number; // Número de días restantes
  hours: number; // Número de horas restantes
  minutes: number; // Número de minutos restantes
}
